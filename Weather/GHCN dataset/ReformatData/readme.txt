1. Launch getStationsId. This makes search of all active US stations_id in the dataset
2.1 Launch reformatDataByStations. This reformats the data by stations id folder	
2.2 FormSequenceGHCN - form the map structure with station as a key, and all measurements as a values and saves it in Sequence Format files(can be used as pre-step for reformatDataByStations)
